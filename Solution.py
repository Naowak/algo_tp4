import Camion

class Solution :
	def __init__(self, camions) :
		if not isinstance(camions, set)
			raise Exception("camion isn't a set")
		_camions = camions
		_cout = 0

	def get_camion(self, camion) :
		return 